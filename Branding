<!-- Silvante Innovations - HumanToken Beta Signup Form -->
<form 
    action="https://formspree.io/f/xblwreja" 
    method="POST" 
    id="betaForm"
    style="max-width:400px; margin:0 auto; background:#1e293b; padding:24px; border-radius:16px; border:1px solid #334155; font-family:Inter, sans-serif; box-shadow:0 8px 32px rgba(59,130,246,0.2);"
>
  <h2 style="color:#f1f5f9; text-align:center; margin-bottom:20px; font-family:'Plus Jakarta Sans',sans-serif;">
    Join the HumanToken Beta
  </h2>
  
  <label for="email" style="color:#cbd5e1; display:block; margin-bottom:8px; font-weight:500;">
    Email Address
  </label>
  <input 
      type="email" 
      name="email" 
      id="email" 
      placeholder="you@example.com" 
      required 
      style="width:100%; padding:14px; margin-bottom:16px; border-radius:12px; border:1px solid #334155; background:#0b1220; color:white; font-size:14px;"
  />
  
  <!-- Hidden fields -->
  <input type="hidden" name="product" value="HumanToken">
  <input type="hidden" name="company" value="Silvante Innovations">
  
  <button type="submit" 
          style="width:100%; padding:14px; background: linear-gradient(135deg, #3b82f6, #2563eb); color:white; border:none; border-radius:12px; cursor:pointer; font-weight:600; font-size:16px; transition: all 0.3s ease;">
    Join Beta
  </button>

  <!-- Messages -->
  <p id="successMessage" style="display:none; color:#22c55e; margin-top:12px; text-align:center; font-weight:500;">
    Thanks — we'll email when beta opens.
  </p>
  <p id="errorMessage" style="display:none; color:#ef4444; margin-top:12px; text-align:center; font-weight:500;">
    There was an error. Try again later.
  </p>

  <p style="color:#64748b; font-size:12px; margin-top:12px; text-align:center;">
    No spam. You can unsubscribe any time.
  </p>
</form>

<script>
  const form = document.getElementById('betaForm');
  const successMsg = document.getElementById('successMessage');
  const errorMsg = document.getElementById('errorMessage');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    successMsg.style.display = 'none';
    errorMsg.style.display = 'none';

    const data = new FormData(form);

    try {
      const res = await fetch(form.action, {
        method: 'POST',
        body: data,
        headers: { 'Accept': 'application/json' }
      });

      if(res.ok){
        successMsg.style.display = 'block';
        form.reset();
      } else {
        errorMsg.style.display = 'block';
      }
    } catch(err){
      errorMsg.style.display = 'block';
    }
  });
</script>
